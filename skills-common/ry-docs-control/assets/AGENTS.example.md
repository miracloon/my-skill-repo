<!--
=== 写作指导（本区块不进入最终文档）===

【文档定位】
AGENTS.md 是项目的 AI 入口文件——AI 进入项目时读的第一个文件。
它不是内容最丰富的文档，而是信号最密集的：让 AI 在最短时间内知道
"在这个项目里我该怎么行动、去哪了解更多"。

【核心职责】
最小识别信息 + 硬性约束 + 导航路由。仅此三项。

【写作原则】
- 极简——能用一行说清的不用三行
- 不替代——完整信息在对应的核心文档中，这里只做摘要和导航
- 入口感——AI 读完后应知道"去哪了解更多"，而不是"已经了解完了"
- 参考体量：30–80 行。超出时检查是否在入口文档中替代了核心文档承担正文

【各栏目 放/不放 速查】

| 栏目 | 放什么 | 不放什么 |
|------|-------|---------|
| AI 启动规则 | 读取顺序、路由指引、一句话边界声明 | 完整项目说明、详细行为规则 |
| 硬性约束 | AI 必须遵守的技术硬约束（语言、工具、环境、禁止项） | 偏好、建议、设计理念——偏好归 INTENT.md |
| 快速环境摘要 | 进入项目后必须立刻知道的环境事实（关键目录、主入口） | 完整目录结构——那归 SPEC.md |
| 按需命令 | 最常用命令的导航索引（一行一条） | 完整操作说明——那归 README.md |
| Skill 导航 | skill 模块的入口引用 | skill 的详细使用说明 |
| 文档导航 | 控制面文档 + 项目中已有的重要 AI 文档 | 所有文件的罗列 |
| 备注 | 极少数容易被忽略的入口级提醒 | 长篇说明、设计讨论 |

【自检清单】
生成或更新 AGENTS.md 后，快速验证：
- [ ] 全文是否在 30–80 行以内？超出了说明可能替代了核心文档
- [ ] 每个栏目是否只做了摘要/导航，而不是展开了完整说明？
- [ ] 硬性约束中是否只写了"必须"级别的约束，没有掺入偏好？
- [ ] 文档导航表是否覆盖了所有已存在的控制面文档？
- [ ] 可选栏目中，没有内容的是否已删除（而非留空）？

=== 写作指导结束 ===
-->

# {项目名称}

{一句话项目定位。简明扼要，不展开背景和决策逻辑——完整意图详见 [`docs/INTENT.md`](docs/INTENT.md)。}

---

## AI 启动规则

- 先读本文件，再按文档导航进入主链。
- 本文件只提供入口级约束、最小识别信息和导航；不在此展开完整项目说明。
- 若需要理解项目为什么这样设计，去读 [`docs/INTENT.md`](docs/INTENT.md)。
- {若项目存在 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)，保留：需要理解全局逻辑组件、职责、依赖、接口或数据所有权时读取它。否则删除本条。}
- 若需要理解项目如何运转，去读 [`docs/WORKFLOW.md`](docs/WORKFLOW.md)。
- 若需要理解结构、目录与边界，去读 [`docs/SPEC.md`](docs/SPEC.md)。
- 若只是一次性小任务或普通问答，不要自动展开为完整的开发流程。

---

## 硬性约束

{列出 AI 在本项目中必须遵守的硬性技术约束，例如语言、运行环境、包管理器、关键工具、不可替换依赖、禁止使用的工具等。只写"必须"级别的约束，偏好和建议归 `docs/INTENT.md`。}

{向后兼容硬约束声明（如果有的话）。详细策略详见 [`docs/SPEC.md`](docs/SPEC.md)。如果项目无此约束，删除本条。}

---

## 快速环境摘要（可选）

{仅保留 AI 进入项目后必须立刻知道的一屏内环境信息，例如关键目录、主入口、运行上下文、主要服务或部署边界。完整结构详见 `docs/SPEC.md`。若无必要，删除本节。}

---

## 按需命令 / 自动化摘要（可选）

{仅列出最常用、最关键的命令或自动化入口，用于导航，不展开完整说明。完整操作步骤写在 `README.md` 或项目对应说明中；若无需此摘要，删除本节。}

---

## Skill 导航（可选）

{如果项目需要 skill 模块，在控制文档构建完成后填写具体 skill 说明；如果项目无需 skill，删除本节。}

---

## 文档导航

| 文档 | 定位 | 什么时候读 |
| --- | --- | --- |
| [`CLAUDE.md`](CLAUDE.md) | Claude 专属补丁 | 由 Claude Code 自动加载时，读取其对 AGENTS 的引用和专属补充 |
| [`docs/INTENT.md`](docs/INTENT.md) | 项目意图 | 需要理解项目目标、边界、取舍、长期方向时 |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)（若存在） | 全局逻辑架构 | 需要理解组件职责、依赖、接口、数据所有权和跨模块不变量时 |
| [`docs/WORKFLOW.md`](docs/WORKFLOW.md) | 运转拓扑 | 需要理解组件所处环节、运行环境、上下游影响时 |
| [`docs/SPEC.md`](docs/SPEC.md) | 结构与规范 | 需要理解目录结构、模块边界、命名与维护规则时 |
| [`docs/development/DEV_WORKFLOW.md`](docs/development/DEV_WORKFLOW.md)（若存在） | 阶段性开发文档治理 | 需要交接、State、active/history 生命周期或开发材料事实身份时 |

---

## 备注（可选）

{写入本项目最容易被 AI 忽略、但又不适合放进上面各节的极少数入口级提醒。若没有，删除本节。}
