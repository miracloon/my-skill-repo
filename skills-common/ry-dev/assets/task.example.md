# [任务名称]

> Baseline: [编译本文件时的 commit]
> Design: [可选；相对路径]

## 用户意图

最终要达成的操作、心智或组织结构，用用户视角表达，不是技术目标。执行方在本文件没有写到的岔路上靠它校准。

## 目标与范围

本次要改变什么。

## 边界

不做什么。包含讨论中已被否决的方向及其理由——这类信息在执行上下文中不可见，也无法从目标推断。

约束带上放弃条件或理由：软约束说明什么情况下可以不做，硬约束说明它为什么硬。不打算规定的地方显式交出判断权。

## 输入与现状

相关文件、当前实现事实、已有约定。项目中可以自行查明的内容不复述。

## 推进思路

可选。只在存在真实依赖顺序时给出：每步完成后达到什么状态，以及为什么是这个顺序。不写文件级步骤清单。

## 验收

可观察的状态、可运行的命令或可检查的产物。它同时回答做到哪算完成、做到哪就该停。

## 完成后

更新对应的 `state.md` 并提交。简述实际做法、与本文档的偏离及原因、未完成项。
